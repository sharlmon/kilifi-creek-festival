{
  "name": "kilifi-creek-festival",
  "private": true,
  "type": "module",
  "scripts": {"dev": "nuxt dev --host 127.0.0.1", "build": "nuxt build", "build:pages": "nuxt build --preset github_pages && node scripts/prepare-pages.mjs", "generate": "nuxt generate", "preview": "nuxt preview", "postinstall": "nuxt prepare", "check": "node scripts/check.mjs"},
  "dependencies": {"nuxt": "4.5.2", "vue": "^3.5.30", "vue-router": "^5.0.4"},
  "engines": {"node": ">=22.19.0"}
}
